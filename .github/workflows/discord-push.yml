on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Delete previous Discord message (if exists)
        run: |
          if [ -f last_message_id.txt ]; then
            last_id=$(cat last_message_id.txt)
            curl -X DELETE "https://discord.com/api/webhooks/<webhook_id>/<webhook_token>/messages/$last_id"
          fi

      - name: Send new Discord message
        id: send_discord
        run: |
          response=$(curl -H "Content-Type: application/json" -X POST -d '{"content":"New commit!","username":"Dan Bot"}' "https://discord.com/api/webhooks/<webhook_id>/<webhook_token>?wait=true")
          echo $response | jq -r '.id' > last_message_id.txt

      - name: Upload last message ID for next run
        uses: actions/upload-artifact@v4
        with:
          name: last_message_id
          path: last_message_id.txt

